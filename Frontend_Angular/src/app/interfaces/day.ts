export interface Day {
  isCurrentMonth: boolean;
    number: number;
    year: number;

    month: string;
    monthIndex: number;

    weekDayName: string;
    weekDayNumber: number;

    weekNumber: number;
}
